# Excel temp lock files
~$*.xlsx

# Raw source spreadsheets are kept private by default (KPI files, the budget
# file, the generated 2024 file). The dashboard already embeds (encrypted) the
# data it needs, so these are never required to view the page.
*.xlsx

# Python cache
__pycache__/

# Claude Code local config
.claude/
